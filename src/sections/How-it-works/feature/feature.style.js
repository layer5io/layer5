import styled from "styled-components";

const Howitworks = styled.section`
.root {
    & > h4 {
      margin-top: 0;
    }
  
    & > p {
      margin-bottom: 2px;
    }
  }
`;

export default Howitworks;