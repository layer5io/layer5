import styled from "styled-components";
import { BlogPageWrapper } from "../../Blog/Blog-grid/blogGrid.style"; // added this to show we can import CSS from toher sections
export const LearnLabsWrapper = styled.div`
    ${BlogPageWrapper};
`;
