---
title: Pagination Code
component: pagination
description: The Pagination component helps users navigate through multiple pages of content. It provides a clear interface for moving between pages and understanding the current position within a larger dataset.
---

import { useState } from "react";
import { Pagination } from "@sistent/sistent";

export const BasicPaginationCodeDemo = () => {
  const [currentPage, setCurrentPage] = useState(1);
  const handlePageChange = (event, value) => setCurrentPage(value);
  return (
    <Pagination count={10} page={currentPage} onChange={handlePageChange} />
  );
};

<a id="Basic Pagination">
  <h3>Basic Pagination</h3>
</a>

<div className="showcase">
  <div className="items">
    <ThemeWrapper>
      <BasicPaginationCodeDemo />
    </ThemeWrapper>
  </div>
  <CodeBlock name="basic-pagination" collapsible code={`
    <Pagination 
      count={10} 
      page={1} 
      onChange={(event, page) => handleChange(page)} 
    />
  `} />
</div>

<a id="Sizes">
  <h3>Pagination Sizes</h3>
</a>

<div className="showcase">
  <div className="items">
    <ThemeWrapper>
      <div style={{ display: "flex", flexDirection: "column", gap: "1rem" }}>
        <Pagination size="small" count={10} />
        <Pagination size="medium" count={10} />
        <Pagination size="large" count={10} />
      </div>
    </ThemeWrapper>
  </div>
  <CodeBlock name="pagination-sizes" collapsible code={`
    <Pagination size="small" count={10} />
    <Pagination size="medium" count={10} />
    <Pagination size="large" count={10} />
  `} />
</div>

<a id="Styling">
  <h3>Custom Styling</h3>
</a>

<div className="showcase">
  <div className="items">
    <ThemeWrapper>
      <Pagination count={10} color="primary" variant="outlined" shape="rounded" />
    </ThemeWrapper>
  </div>
  <CodeBlock name="styled-pagination" collapsible code={`
    <Pagination
      count={10}
      color="primary"
      variant="outlined"
      shape="rounded"
    />
  `} />
</div>

<a id="Outline">
  <h3>Pagination with Outline</h3>
</a>

<div className="showcase">
  <div className="items">
    <ThemeWrapper>
      <Pagination count={10} variant="outlined" color="secondary" />
    </ThemeWrapper>
  </div>
  <CodeBlock name="pagination-outline" collapsible code={`
    <Pagination
      count={10}
      variant="outlined"
      color="secondary"
    />
  `} />
</div>
