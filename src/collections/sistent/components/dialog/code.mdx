---
title: Dialog Code
component: dialog
description: Dialog components provide modal windows that focus user attention on specific tasks or information. They overlay the main content and require user interaction before returning to the underlying interface.
---

import { useState } from "react";
import { Dialog, DialogTitle, DialogContent, DialogActions, Button, Input } from "@sistent/sistent";

export const BasicDialogDemo = () => {
  const [open, setOpen] = useState(false);
  const handleOpen = () => setOpen(true);
  const handleClose = () => setOpen(false);

  return (
    <>
      <Button variant="contained" onClick={handleOpen}>Open Dialog</Button>
      <Dialog open={open} onClose={handleClose}>
        <DialogTitle>Confirm Action</DialogTitle>
        <DialogContent>
          Do you want to proceed with this action?
        </DialogContent>
        <DialogActions>
          <Button variant="contained" onClick={handleClose}>Cancel</Button>
          <Button variant="contained" color="primary" onClick={handleClose}>
            Confirm
          </Button>
        </DialogActions>
      </Dialog>
    </>
  );
};

export const FullScreenDialogDemo = () => {
  const [open, setOpen] = useState(false);
  const handleOpen = () => setOpen(true);
  const handleClose = () => setOpen(false);

  return (
    <>
      <Button variant="contained" onClick={handleOpen}>Open Full-Screen Dialog</Button>
      <Dialog
        open={open}
        onClose={handleClose}
        fullScreen
        maxWidth="lg"
      >
        <DialogTitle>Full-Screen Dialog</DialogTitle>
        <DialogContent>
          <p>
            This dialog stretches to full screen. Use it when the user's
            full attention is needed for complex tasks or on mobile devices.
          </p>
        </DialogContent>
        <DialogActions style={{ justifyContent: "flex-start", paddingLeft: "24px" }}>
          <Button variant="contained" onClick={handleClose}>Cancel</Button>
          <Button variant="contained" color="secondary" onClick={handleClose}>Delete</Button>
        </DialogActions>
      </Dialog>
    </>
  );
};

export const FormDialogDemo = () => {
  const [open, setOpen] = useState(false);
  const handleOpen = () => setOpen(true);
  const handleClose = () => setOpen(false);

  return (
    <>
      <Button variant="contained" onClick={handleOpen}>Open Form Dialog</Button>
      <Dialog open={open} onClose={handleClose}>
        <DialogTitle>Subscribe</DialogTitle>
        <DialogContent>
          <div style={{ display: "flex", flexDirection: "column", gap: "1rem", minWidth: "300px" }}>
            <Input
              label="Email Address"
              type="email"
              placeholder="you@example.com"
              required
            />
            <Input
              label="Name"
              type="text"
              placeholder="John Doe"
              required
            />
          </div>
        </DialogContent>
        <DialogActions>
          <Button variant="contained" onClick={handleClose}>Cancel</Button>
          <Button variant="contained" color="primary" onClick={handleClose}>
            Subscribe
          </Button>
        </DialogActions>
      </Dialog>
    </>
  );
};

The `Dialog` component is essential for creating focused user experiences that require immediate attention. It provides a controlled way to present forms, confirmations, and detailed information without navigating away from the current context.

<a id="Basic Dialog">
  <h2>Basic Dialog</h2>
</a>

A simple modal dialog with title, content, and action buttons. This is the most common pattern for confirmations and simple interactions.

<div className="showcase">
  <div className="items">
    <ThemeWrapper>
      <div style={{ display: 'flex', justifyContent: 'center', width: '100%' }}>
        <BasicDialogDemo />
      </div>
    </ThemeWrapper>
  </div>
  <CodeBlock name="dialog-basic" collapsible code={`const [open, setOpen] = useState(false);

const handleOpen = () => setOpen(true);
const handleClose = () => setOpen(false);

<Button onClick={handleOpen}>Open Dialog</Button>
<Dialog open={open} onClose={handleClose}>
  <DialogTitle>Confirm Action</DialogTitle>
  <DialogContent>
    Do you want to proceed with this action?
  </DialogContent>
  <DialogActions>
    <Button onClick={handleClose}>Cancel</Button>
    <Button color="primary" onClick={handleClose}>
      Confirm
    </Button>
  </DialogActions>
</Dialog>`} />
</div>

<a id="Full Screen Dialog">
  <h2>Full-Screen Dialog</h2>
</a>

Full-screen dialogs are useful for complex forms or when you need the user's complete attention. They take up the entire viewport and are ideal for mobile devices.

<div className="showcase">
  <div className="items">
    <ThemeWrapper>
      <div style={{ display: 'flex', justifyContent: 'center', width: '100%' }}>
        <FullScreenDialogDemo />
      </div>
    </ThemeWrapper>
  </div>
  <CodeBlock name="dialog-fullscreen" collapsible code={`const [openFull, setOpenFull] = useState(false);

<Button onClick={() => setOpenFull(true)}>
  Open Full-Screen Dialog
</Button>
<Dialog
  open={openFull}
  onClose={() => setOpenFull(false)}
  fullScreen
  maxWidth="lg"
>
  <DialogTitle>Full-Screen Dialog</DialogTitle>
  <DialogContent>
    <p>
      This dialog stretches to full screen. Use it when the
      user's full attention is needed.
    </p>
  </DialogContent>
  <DialogActions>
    <Button onClick={() => setOpenFull(false)}>Cancel</Button>
    <Button color="primary" onClick={() => setOpenFull(false)}>
      Save
    </Button>
  </DialogActions>
</Dialog>`} />
</div>

<a id="Form Dialog">
  <h2>Dialog with Form</h2>
</a>

Dialogs can contain forms for collecting user input. This pattern is common for creating, editing, or subscribing workflows that don't require a full page.

<div className="showcase">
  <div className="items">
    <ThemeWrapper>
      <div style={{ display: 'flex', justifyContent: 'center', width: '100%' }}>
        <FormDialogDemo />
      </div>
    </ThemeWrapper>
  </div>
  <CodeBlock name="dialog-form" collapsible code={`const [openForm, setOpenForm] = useState(false);

<Button onClick={() => setOpenForm(true)}>
  Open Form Dialog
</Button>
<Dialog open={openForm} onClose={() => setOpenForm(false)}>
  <DialogTitle>Subscribe</DialogTitle>
  <DialogContent>
    <div style={{ display: "flex", flexDirection: "column", gap: "1rem" }}>
      <Input
        label="Email Address"
        type="email"
        placeholder="you@example.com"
        required
      />
      <Input
        label="Name"
        type="text"
        placeholder="John Doe"
        required
      />
    </div>
  </DialogContent>
  <DialogActions>
    <Button onClick={() => setOpenForm(false)}>Cancel</Button>
    <Button color="primary" onClick={() => setOpenForm(false)}>
      Subscribe
    </Button>
  </DialogActions>
</Dialog>`} />
</div>
